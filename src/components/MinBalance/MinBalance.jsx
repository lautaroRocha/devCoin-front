import React from 'react';

const MinBalance = ( ) => {
    return (
        <div>
            <span>Dinero disponible:</span>
            <span>$6000</span>
        </div>
    );
}

export default MinBalance;
